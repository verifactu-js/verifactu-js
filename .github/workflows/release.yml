name: Release

on:
  push:
    tags:
      - '@verifactu-js/*@*'
  workflow_dispatch:
    inputs:
      package:
        description: 'Package to publish (directory under packages/)'
        required: true
        default: core
      dry_run:
        description: 'Run npm publish --dry-run only'
        type: boolean
        default: true

# `id-token: write` is what makes npm provenance possible: npm trusts a short-lived OIDC token
# minted by GitHub for this workflow run, so no long-lived NPM_TOKEN is stored anywhere.
# Requires npm >= 11.5.1 and a package whose repository field matches this repo.
#
# Provenance is requested with the `--provenance` FLAG below, deliberately not with
# `publishConfig.provenance: true` in package.json. That setting applies to every publish,
# including one run from a laptop, where there is no CI provider to attest anything and npm
# fails with `EUSAGE: Automatic provenance generation not supported for provider: null`.
# Keeping it as a flag means local publishes work and CI publishes are attested.
# Do not move it back into package.json.
permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    environment: npm-publish
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 11.1.2

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          cache: pnpm

      # Trusted publishing needs a recent npm; the one bundled with Node may lag behind.
      - name: Update npm
        run: npm install -g npm@latest

      - name: Resolve package directory
        id: pkg
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "dir=packages/${{ inputs.package }}" >> "$GITHUB_OUTPUT"
          else
            # Tag shape: @verifactu-js/<name>@<version>
            name="${GITHUB_REF_NAME#@verifactu-js/}"
            echo "dir=packages/${name%@*}" >> "$GITHUB_OUTPUT"
          fi

      # npm answers 404 both when the package does not exist and when the credentials are not
      # authorised for that name, and the message distinguishes neither. This says which, and it
      # runs first: there is no point linting, testing and building for four minutes to be turned
      # away by the registry at the end.
      #
      # The first version of a new package cannot be published over OIDC at all — the trusted
      # publisher is configured on the package's own settings page, which does not exist until the
      # package does (npm/cli#8544, open since 2025-09-01). Needs no dependencies: plain `fetch`.
      #
      # Skipped on a dry run, which is exactly how you check the packaging of a package that is
      # not on the registry yet.
      - name: Publishable at all
        if: '!inputs.dry_run'
        run: node scripts/check-first-publish.mjs ${{ steps.pkg.outputs.dir }}

      - run: pnpm install --frozen-lockfile

      - name: Verify before publishing
        run: |
          pnpm lint
          pnpm typecheck
          pnpm test:coverage
          pnpm build

      - name: Publish (dry run)
        if: inputs.dry_run
        working-directory: ${{ steps.pkg.outputs.dir }}
        run: npm publish --dry-run --provenance --access public

      - name: Publish
        if: '!inputs.dry_run'
        working-directory: ${{ steps.pkg.outputs.dir }}
        run: npm publish --provenance --access public
